#设置端口号
server:
  port: 8082
  servlet:
    context-path: /hu
spring:
  # 数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/huanying?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: qi123

# 显示日志
logging:
  level:
    com:
      tian: debug
  # 日志输出到文件
  file:
    name: f:/log/user-server.log

# 图片上传保存路径
upload:
  path: F:\myweb\huanyingol\public\images
#  path: /home/vue/dist/images
